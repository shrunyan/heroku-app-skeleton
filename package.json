{
  "name": "heroku-app-skeleton",
  "description": "Skeleton application for deploying on Heroku. Using Backbone, Marionette and their dependencies.",
  "version": "0.0.1",
  "author": "shrunyan",
  "scripts": {
    "start": "npm install && npm run serve && npm run watch",
    "postinstall": "npm run build",
    "build": "npm run clean && npm run build-js && build-css",
    "build-js": "",
    "build-css": "",
    "clean": "rm -rf dist && mkdir dist",
    "server": "iojs app/node_modules/server/server.js",
    "watch": ""
  },
  "dependencies": {
    "backbone": "^1.1.2",
    "backbone.babysitter": "^0.1.6",
    "backbone.marionette": "^2.4.1",
    "backbone.wreqr": "^1.3.1",
    "jquery": "^2.1.3",
    "underscore": "^1.8.2"
  }
}
